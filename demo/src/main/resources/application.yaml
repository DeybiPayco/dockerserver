spring:
  datasource:
    url: jdbc:sqlserver://13.222.141.60:1433;databaseName=productos_db;encrypt=true;trustServerCertificate=true
    username: sa
    password: Agro1234!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update       # crea/actualiza tablas automáticamente
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect

server:
  port: 8080
